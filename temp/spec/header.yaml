swagger: '2.0'
info:
  title: Test API
  version: '{{version}}'
host: '{{host}}'
basePath: /api/v2
schemes:
  - https
consumes:
  - application/json
produces:
  - application/json
securityDefinitions:
  token:
    name: X-Access-Token
    type: apiKey
    in: header